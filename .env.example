# Mật khẩu đăng nhập cho Giáo viên (bắt buộc, tự đặt một mật khẩu mạnh)
TEACHER_PASSWORD=doi-mat-khau-nay

# Chuỗi bí mật để ký session cookie của Giáo viên (bắt buộc, một chuỗi ngẫu nhiên dài)
SESSION_SECRET=doi-chuoi-ngau-nhien-dai-o-day

# Các biến POSTGRES_URL, POSTGRES_URL_NON_POOLING, ... sẽ được Vercel
# tự động thêm vào khi bạn bấm "Connect Store" -> Postgres trong dashboard.
# Không cần tự nhập tay khi deploy trên Vercel.
